class Solution {
    public boolean uniformArray(int[] nums1) {
        // int nums2[] = new int[nums1.length], count1 = 0, count2 = 0, f = 0;

        // for (int j = 0; j < nums1.length; j++) {
        //     if (nums1[j] % 2 == 0) {
        //         count1++;
        //     }
        // }

        // if (count1 == nums1.length)
        //     return true;

        // for (int j = 0; j < nums1.length; j++) {
        //     if (nums1[j] % 2 != 0) {
        //         f = 1;
        //     }
        // }

        // if (f > 0) {
            
        //     for (int j = 0; j < nums1.length; j++) {

        //         if (nums1[j] % 2 == 0) {
        //             if (j + 1 < nums1.length)
        //                 nums2[j] = nums1[j + 1] - nums1[j];
        //             else
        //                 return false;
        //         } else
        //             nums2[j] = nums1[j];
        //     }
        //     for (int j = 0; j < nums2.length; j++) {
        //         if (nums2[j] % 2 != 0)
        //             count2++;
        //     }
        //     if (count2 == nums2.length)
        //         return true;
        // }
        // return false;

        return true;
    }
}